namespace PacketModel.Enums
{
    /// <summary>
    /// Enum for PacketHandler to decide between client operation or server operation
    /// </summary>
    public enum HandlerOperator
    {
        Undefined,
        Client,
        Server
    }
}